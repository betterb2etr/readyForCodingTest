// 내 풀이
#include <iostream>
#include <unordered_map>
#include <string>
using namespace std;

int main() {

    int N;
    cin >> N;

    unordered_map<string, int> runners;
    string name = "";

    //참가자 N명 입력
    for (int i = 0; i < N; i++) {
        name = "";
        cin >> name;
        runners[name]++;         
    }

    // 완주자 
    for (int i = 0; i < N - 1; i++) {
        name = "";
        cin >> name;
        runners[name]--;          
    }

    for (pair<const string, int> &p : runners) {
        if (p.second != 0) {
            cout << p.first << '\n';
            break;
        }
    }

    return 0;
}
